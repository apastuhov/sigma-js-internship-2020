import React from 'react';
import Layout from '../../shared/layout/Layout';

const Chat: React.FC = () => {
  return (
    <Layout PageTitle="Chat">
      <div></div>
    </Layout>
  );
};

export default Chat;
